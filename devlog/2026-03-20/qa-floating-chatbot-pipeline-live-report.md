# 플로팅 챗봇 5요소 파이프라인 실전 검수 판정본

> 이 문서는 실제 브라우저에서 수동 검수를 수행하면서 바로 결과를 기록하는 용도다.
> 체크박스와 메모를 실제 확인 결과 기준으로 채우고, 최종 판정을 명시한다.

- **검수일**: 2026-03-20
- **검수자**:
- **대상 빌드**: `main.e7fa63189005f115.js`
- **대상 URL**:
- **브라우저 / 버전**:
- **검수 환경**: Desktop / Tablet / Mobile 중 선택
- **선택 컬렉션**:
- **검수 범위**: 플로팅 챗봇 최종 답변 파이프라인 (`프롬프트 + 오케스트레이터 + 도구 + 메모리 + 스트리밍 UI`)

## 1. 사전 조건
- [ ] 대상 빌드가 최신 배포본인지 확인했다.
- [ ] 선택 컬렉션이 정상적으로 로드된다.
- [ ] 플로팅 버튼 진입이 정상 동작한다.
- [ ] 기존 14단계 사고과정 UI가 함께 노출된다.

## 2. 시나리오 A — 참고 문헌 검색형
- **질문**: 플라즈마 에칭 관련 최신 논문 핵심 포인트 정리해줘
- **결과 요약**:
- **판정**: Pass / Conditional Pass / Fail

### 체크 항목
- [ ] `최종 답변 구성 요소` 섹션이 노출된다.
- [ ] `프롬프트` 카드가 질문 직후 먼저 진행 중 상태가 된다.
- [ ] `오케스트레이터` 카드에 분류/계획 정보가 보인다.
- [ ] `도구` 카드에 실행 개수 또는 도구명 배지가 갱신된다.
- [ ] `메모리` 카드에 컬렉션/이력 맥락이 반영된다.
- [ ] `스트리밍 UI` 카드가 진행 중 → 완료 상태로 마무리된다.
- [ ] 참고 문헌 카드와 유사도 요약이 생성된다.
- [ ] 최종 답변 카드가 별도 유지된다.

### 메모
- 파이프라인 상태 흐름:
- 참고 문헌 수 / 유사도 요약:
- 이상 징후:

## 3. 시나리오 B — 페이지 이동형
- **질문**: OES 이상 탐지 흐름으로 이동해서 다음 액션까지 정리해줘
- **결과 요약**:
- **판정**: Pass / Conditional Pass / Fail

### 체크 항목
- [ ] 오케스트레이터 카드에 진단 분석 계열 분류가 반영된다.
- [ ] 오케스트레이션 계획 목록이 자연스럽게 보인다.
- [ ] 도구 로그에 navigation 관련 흔적이 남는다.
- [ ] 파이프라인 `도구` 카드와 별도 도구 로그 섹션이 충돌하지 않는다.
- [ ] handoff 성격의 최종 답변이 함께 노출된다.
- [ ] 이동 이후에도 선택 컬렉션 정보가 어색하게 사라지지 않는다.

### 메모
- 분류 / 페이지 / 탭:
- 이동 버튼 또는 안내 상태:
- 이상 징후:

## 4. 시나리오 C — 연속 대화형
- **질문 1**: ICP 식각 조건 예측해줘
- **질문 2**: 방금 조건 기준으로 RF 파워 영향만 더 설명해줘
- **결과 요약**:
- **판정**: Pass / Conditional Pass / Fail

### 체크 항목
- [ ] 두 번째 턴에서 이전 assistant 턴이 접힌다.
- [ ] 최신 assistant 턴만 펼쳐진다.
- [ ] 메모리 카드에 이전 대화 턴 수 또는 최근 맥락이 반영된다.
- [ ] 선택 컬렉션 상태가 두 턴 모두 유지된다.
- [ ] 두 번째 질문에서도 파이프라인 카드 상태가 처음부터 자연스럽게 갱신된다.
- [ ] 긴 응답에서도 자동 스크롤이 자연스럽다.

### 메모
- 1턴 요약:
- 2턴 요약:
- 메모리 반영 확인 포인트:
- 이상 징후:

## 5. 오류 / 취소 / 예외
- **질문 또는 조작**:
- **결과 요약**:
- **판정**: Pass / Conditional Pass / Fail

### 체크 항목
- [ ] 오류 또는 취소 시 사용자 이해 가능한 상태가 노출된다.
- [ ] 다음 질문 재시도 시 파이프라인 카드가 정상 초기화된다.
- [ ] 컬렉션 변경 후 이전 맥락이 과도하게 남지 않는다.

### 메모
- 재현 절차:
- 오류 메시지 / 상태:
- 이상 징후:

## 6. UI / 레이아웃 품질
- [ ] 카드 간 간격이 자연스럽다.
- [ ] 파이프라인 카드 배지 줄바꿈이 과도하게 깨지지 않는다.
- [ ] 긴 참고 문헌/긴 도구 결과에서도 레이아웃이 무너지지 않는다.
- [ ] 최종 답변 카드가 가장 읽기 쉬운 본문 영역으로 유지된다.
- [ ] 좁은 폭에서도 핵심 정보 가독성이 유지된다.

### 메모
- 폭/해상도:
- 가독성 이슈:
- 개선 의견:

## 7. 종합 판정
- [ ] Pass
- [ ] Conditional Pass
- [ ] Fail

### 종합 메모
- 핵심 정상 포인트:
- 주요 이슈:
- 배포 가능 여부:

## 8. 후속 조치
- 조치 1:
- 조치 2:
- 조치 3:
